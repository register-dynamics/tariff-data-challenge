CREATE TABLE commodity_codes(
  "sid" INTEGER,
  "code" TEXT,
  "suffix" TEXT,
  "description" TEXT,
  "validity_start_date" TEXT,
  "validity_end_date" TEXT
);

CREATE TABLE indents(
  "sid" INTEGER,
  "indent" INTEGER,
  "validity_start_date" INTEGER
);